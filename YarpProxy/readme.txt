create new project using web api template
- enable docker etc

then add yarp packages
- add yarp config etc

then also add yarp conditional config code in program.cs

test you can debug both http and https hosted from right inside the container